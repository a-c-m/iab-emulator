// Shared base URLs for the two demo servers Playwright boots (see
// playwright.config.ts). Kept in one place so the config and the specs agree.
export const PLUGIN_URL = "http://localhost:5188";
export const CLEAN_URL = "http://localhost:5189";
