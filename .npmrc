save-exact=true
auto-install-peers=false
# Single-package repo that carries a pnpm-workspace.yaml (to detach from the
# parent base-app workspace, ADR 0005). Deps belong on the root package.
ignore-workspace-root-check=true
